# Web framework
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
httpx==0.27.2

# Transcription — uncomment to enable local Whisper (CPU works; GPU needs CUDA + float16).
# Runs on CTranslate2, no PyTorch required on CPU.
# faster-whisper==1.0.3

# LLM providers (install only what you use; Ollama needs no SDK — it's plain HTTP)
# openai==1.51.0
# anthropic==0.39.0
# google-generativeai==0.8.3
